import { useEffect,useState } from "react";
import { useNavigate } from "react-router-dom";
import { Clock3,ArrowRight,History,MessageSquare,BarChart3,Map } from "lucide-react";
import { getAnalyses } from "../services/api";
export default function HistoryPage(){const nav=useNavigate();const [items,setItems]=useState([]),[error,setError]=useState("");useEffect(()=>{getAnalyses().then(setItems).catch(e=>setError(e.message))},[]);return <main className="min-h-[calc(100vh-68px)] bg-[#fafafa]"><div className="mx-auto max-w-5xl px-5 py-10"><div className="flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-indigo-50 text-indigo-600"><History size={18}/></span><div><p className="text-xs font-bold uppercase tracking-wider text-indigo-600">SAVED WORK</p><h1 className="font-serif text-3xl font-bold">Your journey</h1></div></div><p className="mt-2 text-sm text-gray-500">History is an archive. Your current Chat, Skill Gap and Roadmap pages load their data directly from your account.</p>{error&&<p className="mt-5 rounded-lg bg-red-50 p-3 text-xs text-red-600">{error}</p>}<div className="mt-7 space-y-4">{items.length===0?<div className="rounded-xl border bg-white p-8 text-sm text-gray-500">No saved assessments yet.</div>:items.map(x=><article key={x.session_id} className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="flex items-center gap-2 text-sm font-semibold"><span>{x.role_id.replaceAll("-"," ")}</span><span className={`rounded-full px-2 py-1 text-[9px] font-bold uppercase ${x.status==="complete"?"bg-green-50 text-green-700":"bg-gray-100 text-gray-500"}`}>{x.status}</span></div><p className="mt-2 flex items-center gap-1.5 text-xs text-gray-400"><Clock3 size={12}/>{new Date(x.updated_at).toLocaleString()}</p></div><ArrowRight size={16} className="text-gray-300"/></div>{x.status==="complete"&&<div className="mt-5 flex flex-wrap gap-2"><button onClick={()=>nav(`/chat?session=${x.session_id}`)} className="flex items-center gap-1.5 rounded-xl border px-3 py-2 text-xs font-semibold"><MessageSquare size={13}/> Chat</button><button onClick={()=>nav("/skill-gap")} className="flex items-center gap-1.5 rounded-xl border px-3 py-2 text-xs font-semibold"><BarChart3 size={13}/> Skill Gap</button><button onClick={()=>nav("/roadmap")} className="flex items-center gap-1.5 rounded-xl border px-3 py-2 text-xs font-semibold"><Map size={13}/> Current Roadmap</button></div>}</article>)}</div></div></main>}
