User selects a role → guided Q&A calibrates skill levels → skill gap analysis → phased roadmap.
Auth is optional — full flow works without an account; logging in enables saving/returning to a plan later.
